# GitHub Configuration
# Choose one authentication method:
GITHUB_TOKEN=your_github_personal_access_token_here
# OR use basic authentication (not recommended for production)
# GITHUB_USERNAME=your_github_username
# GITHUB_PASSWORD=your_github_password

# Default repository to use (format: owner/repo)
DEFAULT_REPO=Dhruv1969Karnwal/up-test-rel

# Server Configuration
MIDDLEWARE_HOST=localhost
MIDDLEWARE_PORT=8000

# Performance Settings
CACHE_TTL=300  # Cache time-to-live in seconds (5 minutes)
RATE_LIMIT=100  # Rate limit requests per minute

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Development Settings
DEBUG=false
RELOAD=true